<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wembleymoviesapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_wembleystudios"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_wembleystudios_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WembleyMoviesApp">
        <activity
            android:name=".ui.view.activities.DetailMovieActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>